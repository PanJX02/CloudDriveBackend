<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.panjx.clouddrive.mapper.UserMapper">
    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" resultType="com.panjx.clouddrive.pojo.User">
        SELECT
            *
        FROM 
            user
        WHERE 
            user_name = #{username}
    </select>

    <!-- 注册新用户 -->
    <insert id="add" parameterType="com.panjx.clouddrive.pojo.User">
        INSERT INTO user (user_name, nick_name, password, join_time, identity)
        VALUES (#{userName}, #{nickName}, #{password}, #{joinTime}, #{identity})
    </insert>
</mapper> 